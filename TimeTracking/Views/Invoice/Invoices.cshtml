@model TimeTracking.Models.Multiplemodels
@{

    ViewBag.Title = "Invoice";
    
    long invoiceId = Model.InvoiceModel.Id;
}
@Scripts.Render("~/Scripts/jquery-1.10.2.min.js")
<script type="text/javascript">
    function GenerateInvoice(id, qboId) {
        alert("Gen called " + id + "qbo id " + qboId);
        $.ajax({
            url: '@Url.Action("Save", "Invoice")',
            type: 'POST',
            data: { id: id, qboId: qboId },
            success: function (data) {
                debugger;
                //LoadResult(data);
            }
        });
    }
</script>

<div ng-class="row">
    <div class="alert alert-success alert-dismissible" role="alert" ng-show="showAlert">
        <button type="button" class="close" data-dismiss="alert">
            <span aria-hidden="true">&times;</span><span class="sr-only">Close</span>
        </button>
       @Model.InvoiceModel.AlertMessage;
    </div>
</div>
<div ng-class="row">
    <h3>Step 3: Generate Invoices</h3>
</div>

<div id="invPending">
    <div ng-class="row">
        <hr>
        <h4><em>Total @Model.InvoiceModel.InvoicePending:@Model.InvoiceModel.InvoicePendingLength</em></h4>
        <hr>
    </div>
    <div ng-class="row">
        <table class="table table-striped table-bordered">

            <tbody>
                <tr ng-repeat-end="" ng-if="invoice.showServiceItemSummaries">
                    <td></td>
                    <td colspan="4">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Employee</th>
                                    <th>Service Item</th>
                                    <th>Date</th>
                                    <th>Hours</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @for (int i = 0; i < @Model.InvoiceModel.InvoicePendingList.Count; i++)
                                {
                                    <tr>
                                        <td>@Model.InvoiceModel.InvoicePendingList[i].Employee</td>
                                        <td>@Model.InvoiceModel.InvoicePendingList[i].Item</td>
                                        <td>@Model.InvoiceModel.InvoicePendingList[i].Date</td>
                                        <td>@Model.InvoiceModel.InvoicePendingList[i].Hours</td>
                                        <td>@Html.ActionLink("Generate", "Save", "Invoice", new { id = invoiceId, qboId = @Model.InvoiceModel.InvoicePendingList[i].QboId }, new { @class = "btn btn-default btn-sm ng-binding" })</td>
                                        <td><a class="btn btn-default btn-sm ng-binding" href="#" onclick="GenerateInvoice(@invoiceId,@Model.InvoiceModel.InvoicePendingList[i].QboId);">Generate</a></td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>


</div>
@*<div id="invCreated">
    <div ng-class="row">
        <hr>
        <h4><em>Total @Model.InvoiceModel.InvoicePending:@Model.InvoiceModel.InvoicePendingLength</em></h4>
        <hr>
    </div>
    <div ng-class="row">
        <table class="table table-striped table-bordered">

            <tbody>
                <tr ng-repeat-end="" ng-if="invoice.showServiceItemSummaries">
                    <td></td>
                    <td colspan="4">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Employee</th>
                                    <th>Service Item</th>
                                    <th>Date</th>
                                    <th>Hours</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                @for (int i = 0; i < @Model.InvoiceModel.InvoicePendingList.Count; i++)
                                {
                                    <tr >
                                        <td>@Model.InvoiceModel.InvoicePendingList[i].Employee</td>
                                        <td>@Model.InvoiceModel.InvoicePendingList[i].Item</td>
                                        <td>@Model.InvoiceModel.InvoicePendingList[i].Date</td>
                                        <td>@Model.InvoiceModel.InvoicePendingList[i].Hours</td>
                                        <td>@Model.InvoiceModel.InvoicePendingList[i].Action</td>
                                    </tr>
                                }
                              
                            </tbody>
                        </table>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>


</div>*@